name: Build Fox Terminal APK

on:
  push:
    branches: [ main ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true   # Força Node 24 (remove warnings futuros)

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build APK with Buildozer
        run: |
          echo "=== Atualizando Cython para versão estável ==="
          pip install --upgrade "Cython==0.29.36"

          echo "=== Iniciando build do APK (pode demorar 10\~20 minutos) ==="
          yes | buildozer -v android debug

          echo "=== Verificando APK gerado ==="
          ls -la bin/ || true
          find . -name "*.apk" -type f || true
        shell: bash
        timeout-minutes: 25

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: fox-terminal-apk
          path: bin/*.apk
          if-no-files-found: warn
